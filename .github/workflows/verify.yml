name: Verify

on:
  push:
    branches: ["main"]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Setup Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.12"

      - name: Run JavaScript tests
        run: node --test tests/tracker_helpers.test.mjs tests/email_consolidator.test.mjs

      - name: Run Python tests
        run: python -m unittest discover -s tests -p "test_*.py"

      - name: Showcase voice guard
        run: python scripts/check_showcase_voice.py

      - name: Generate consistency reports
        run: |
          python scripts/verify_repo_consistency.py
          python scripts/repo_sweep.py
